# Security (required in production)
JWT_SECRET=your-secret-key-here
JWT_ALGORITHM=HS256

# Postgres — optional; app uses SQLite by default
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=shipper
POSTGRES_PASSWORD=shipper
POSTGRES_DB=shipper
